# 모바일 실기기 테스트 시 LAN IP로 접속할 때만 필요 (예: 192.168.0.10)
# 비워두면 next.config.ts에서 allowedDevOrigins 설정 자체가 생략됨
DEV_LAN_ORIGIN=

# Supabase 프로젝트 설정 → API에서 확인 (anon/public key는 RLS로 보호되므로 브라우저 노출 가능)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
